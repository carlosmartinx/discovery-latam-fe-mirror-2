import React from 'react';
import {
  Root, DescCard, Title,
} from './styled';

export const ArticleLayout = () => (
  <Root>
    <DescCard>
      <Title>¿Son adictivos los tatuajes?</Title>
    </DescCard>
  </Root>
);

export default ArticleLayout;
