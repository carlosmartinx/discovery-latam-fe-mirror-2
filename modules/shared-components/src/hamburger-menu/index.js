/* eslint-disable */
import React from 'react';
import PropTypes from 'prop-types';

const ListItem = props => {
  return <li>{props.value}</li>
}

const Menu = () => (
  <div>
    <section>
      <a href="/">Tu discovery</a>
      <nav>
        
      </nav>
    </section>
  </div>
);

Menu.propTypes = {
};

export default Menu;
